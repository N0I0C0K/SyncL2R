connect_config:
  ip: 127.0.0.1
  username: root
  keyfile: local
  port: 11022
file_sync_config:
  exclude: []
  remote_root_path: '/home/test'
  root_path: .
events:
  push_complete_exec:
    - pwd
  push_start_exec:
    - echo hello world
    - sleep 1
    - pwd
actions:
  -
    name: test
    cmd:
      - ls
      - pwd